dependencies:
  pre:
    - npm install -g npm
machine:
  node:
    version: 5.0.0
    environment:
        REDISCLOUD_URL: redis://rediscloud:1e6OJhYbvshUm6Mr@pub-redis-13907.eu-west-1-1.2.ec2.garantiadata.com:13907
general:
  artifacts:
    - "coverage"
test:
    post:
        - sudo pip install codecov && codecov
